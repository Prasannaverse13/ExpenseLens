## Local properties template — copy to local.properties and fill in.
## local.properties is gitignored, so it never leaves your machine.

## Android SDK location. Windows example:
# sdk.dir=C\:\\Users\\YOURNAME\\AppData\\Local\\Android\\Sdk
# macOS / Linux:
# sdk.dir=/Users/YOURNAME/Library/Android/sdk

## OpenAI API key (for smart bill extraction). Optional — the on-device
## parser is the always-on fallback.
# openai.api.key=sk-proj-xxxxxxxxxxxx
# openai.model=gpt-4o

## Paddle Premium subscription.
## The app opens `paddle.pricing.url` in a Chrome Custom Tab. That page
## hosts the Paddle.js inline checkout; after a successful payment the
## page fires expenselens://premium-callback?status=success and the app
## unlocks Premium locally.
##
## Paddle Billing v2 does NOT support hosted checkout URLs for native
## mobile apps. See PADDLE_SETUP.md for the full setup walkthrough.
# paddle.product.id=pro_xxxxxxxxxxxxxxxxxxxxxx
# paddle.price.id=pri_xxxxxxxxxxxxxxxxxxxxxx
# paddle.pricing.url=https://prasannaverse13.github.io/pricing.html
# paddle.price.usd=4.99
# paddle.portal.url=

## Optional: Paddle Billing v2 SERVER-SIDE API key (pdl_…). Generate
## at https://vendors.paddle.com/authentication-v2 → API keys tab.
## When set, Settings offers a "Verify with Paddle" action that
## confirms an active subscription against Paddle's servers — useful
## when the success-page deep link was blocked or missed.
# paddle.api.key=pdl_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

## Supabase — primary data store (replaces Google Drive as of v1.1).
## URL: Supabase dashboard → Project Settings → API
## anon key: same page → Project API keys → anon public (JWT starting with eyJ...)
## The anon key is safe to ship in the APK — it's the public key
## designed for client apps. RLS on every table enforces
## user_id = auth.uid() isolation.
# supabase.url=https://xxxxxxxxxxxx.supabase.co
# supabase.anon.key=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.xxxxxxxxxxxxxxxxxxxx

## Google OAuth Client ID — needed for Google Sign-In + Supabase exchange.
## Create one in Google Cloud Console → APIs & Services → Credentials
## → Create Credentials → OAuth client ID. Type: "Android" (or "Web
## application" — both work for requestIdToken). Configure with:
##   - Package name: com.expenselens
##   - SHA-1: <your debug keystore SHA-1> (run `keytool -list -v
##     -keystore ~/.android/debug.keystore -storepass android -alias
##     androiddebugkey | grep SHA1`)
## The Client Secret is NOT needed (Supabase verifies the ID token
## against Google's public JWKS, not via shared secret).
## Leave blank to keep working in Drive-only mode (no cloud sync).
# google.web.client.id=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx.apps.googleusercontent.com

# Place `eng.traineddata` (and `osd.traineddata` if you want orientation detection)
# in this directory to enable the Tesseract fallback OCR engine.
#
# Download from: https://github.com/tesseract-ocr/tessdata_fast
#
# Without these files the app still works — the ML Kit primary engine will be
# used and Tesseract will be silently skipped.
